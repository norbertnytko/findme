<%= turbo_stream.replace dom_id(@link) do %> 
  <%= render(partial: 'link', locals: { link: @link, one_pager: @one_pager }) %>
<% end %>

<%= turbo_stream.replace 'preview' do %>
  <%= render partial: 'one_pagers/preview', locals: { one_pager: @one_pager } %>
<% end %>
