<%= turbo_stream.update nested_dom_id(@one_pager, Link.new), "" %>

<%= turbo_stream.prepend "links", render(partial: 'link', locals: { link: @link, one_pager: @one_pager }) %>

<%= turbo_stream.replace 'preview' do %>
  <%= render partial: 'one_pagers/preview', locals: { one_pager: @one_pager } %>
<% end %>
